name: "Publish Artifacts"

on:
  push:
    tags: [ "v*" ]

jobs:
  publish:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v2

      - name: Set up Java for Publishing
        uses: actions/setup-java@v2
        with:
          distribution: 'temurin'
          java-version: 11

      - name: Publish to GitHub Packages
        uses: gradle/gradle-build-action@v2
        with:
          arguments: publish
        env:
          ORG_GRADLE_PROJECT_publishVersion: ${{ github.ref }}
          ORG_GRADLE_PROJECT_gitHubPackagesUsername: ${{ github.actor }}
          ORG_GRADLE_PROJECT_gitHubPackagesPassword: ${{ secrets.GITHUB_TOKEN }}

      - name: Setup Release Environment Variables
        run: |
          echo "RELEASE_NAME=`echo ${{ github.ref }} | sed -e 's/refs\/tags\///'`" >>　$GITHUB_ENV
          echo "VERSION=`echo ${{ github.ref }} | sed -e 's/refs\/tags\/v//'`" >>　$GITHUB_ENV
          echo "ARTIFACT_NAME=${{ github.event.repository.name }}-`echo ${{ github.ref }} | sed -e 's/refs\/tags\/v//'`.zip" >>　$GITHUB_ENV

      - name: Upload to GitHub Release
        run: |
          curl -s -XPOST -H "Authorization: token ${{ secrets.GITHUB_TOKEN }}" \
            -H "Content-Type: application/octet-stream" \
            --upload-file build/distributions/${{ env.ARTIFACT_NAME }} \
            "https://uploads.github.com/repos/${{ github.repository }}/releases/${{ env.RELEASE_NAME }}/assets?name=${{ env.ARTIFACT_NAME" }}
